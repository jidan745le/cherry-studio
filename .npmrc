# pnpm isolated node_modules breaks node-gyp on Windows: MSBuild ends up with an invalid
# path like .pnpm\@paymoapp+...@1.1.2\node-addon-api@5.1.0\... (nothing.vcxproj C1083).
# Hoisted layout matches npm and lets @paymoapp/electron-shutdown-handler compile.
node-linker=hoisted
